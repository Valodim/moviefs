MovieFS

This is a fuse filesystem implemented in python that will, at some point, list
movies from a database in a virtual directory structure.

It's all very alpha and hardly of use, but feel free to look around.
